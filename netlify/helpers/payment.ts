export const updateUsage = async (
  teamId: string,
  usage: number
): Promise<void> => {
  // For the sake of this demonstration, a payment provider is mocked.
  // We could use Stripe or any other payment provider.
  return;
};
